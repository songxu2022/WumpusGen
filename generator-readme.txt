Generator

The generator takes the size and number of pits as input and output a randomly generated game board to the text file "generator_data.txt". 
The visualized game board is stored in "generator_data.txt". The abbreviation is same as node.
In "generator_data.txt", each line is the infor of an individual spot. The format is x-coordinate, y coordinate and label.
